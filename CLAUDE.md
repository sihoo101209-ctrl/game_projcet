# 게임 프로젝트 — 레벨 배치 A/B 테스트

## 이 프로젝트의 목적

상업용 게임이 아니라 **실험 도구**다. 고등학교 자유주제 탐구 활동으로, 방 연결 구조가 플레이어 행동에 미치는 영향을 측정한다. 20명이 A 또는 B 버전을 5분간 플레이하고 로그와 설문을 남긴다.

**최우선 가치는 "재미"가 아니라 "통제된 비교 가능성"이다.**

---

## 절대 지켜야 할 제약

이 규칙을 어기면 데이터 전체가 무효가 된다. "이게 더 나을 것 같은데" 싶어도 아래는 건드리지 말 것.

| 규칙 | 이유 |
|---|---|
| A와 B는 **연결 구조만** 다르다 | 독립변수가 하나여야 인과를 특정할 수 있음 |
| 총 적 수 **양쪽 다 30마리** | 전투량이 다르면 배치 효과를 분리 불가 |
| 보상 **양쪽 다 3개, 같은 방(3·5·7)에** | 동일 |
| 최단 경로 **양쪽 다 시작방 + 8방** | B가 더 길면 "복잡해서"가 아니라 "멀어서" 느려짐 |
| **게임 화면에 미니맵 금지** | 상용 게임의 복잡도 완화 장치를 제거한 대조군이 목적. 맵은 ESC 메뉴 안에서만 |
| **문 위 보상 미리보기 금지** | 동일 (하데스식 장치) |
| **되돌림 복도(101·102·103)에 보상 금지** | 적만 배치 |
| 방 6 → 방 3, 방 102 → 방 2 는 **반드시 일방통행** | 양방향이면 지름길이 생겨 최단 경로 붕괴 |
| **클리어한 방은 적이 다시 나오지 않는다** | 되돌아갔을 때 재전투가 강제되면 이동 비용이 아니라 전투량 차이가 측정됨 |
| 창 크기 **1280×720 고정, 전체화면 금지** | 보이는 범위가 다르면 미니맵만큼 큰 변수 |

---

## 화면 구성

### 타이틀 화면 (씬: Start)

한 화면에 전부. 별도 타이틀 씬을 만들지 말 것.

```
        게임 프로젝트
       레벨 배치 테스트

    기기 번호   [    ]
    참가자 번호 [    ]

         [ 시작 ]

   WASD 이동 · 마우스 클릭 공격
   방의 적을 모두 처치하면 문이 열립니다
   ESC 를 누르면 메뉴가 열립니다

                                   ver. A
```

- **타이틀 화면 우하단에 `ver. A` / `ver. B` 를 작게 표시할 것.** `GameLogger.Instance.mapVersion` 을 그대로 읽어 띄운다. 잘못된 버전으로 빌드했을 때 테스트 도중에라도 발견할 수 있는 유일한 안전장치다
- 기기 번호는 한 번 입력하면 그 실행 내내 유지
- 난이도 선택, 인트로, 스토리 **없음** (읽는 시간이 사람마다 달라 총 플레이시간 오염)
- **ESC 안내는 필수.** 화면에 그만하기 버튼이 없으므로, 이 줄이 없으면 그만두는 방법을 알 수 없어 창을 강제로 닫게 되고 포기 시점이 부정확해진다

### 게임 화면 (씬: Game)

| 위치 | 요소 |
|---|---|
| 좌상단 | 현재 방 번호 (필수) |
| 좌상단 | HP 바 |
| 상단 중앙 | 보스·준보스 체력 바 (해당 방에서만) |
| 상단 1/3 지점 | 아이템 획득 알림 (1.5초 후 페이드아웃) |
| 적 머리 위 | 피격 시 체력 바 표시 → 1초 후 페이드아웃 |

**화면에 그만하기 버튼을 두지 말 것.** 종료는 ESC 메뉴와 사망 화면에서만 가능하다.

**적 체력 표시 규칙**

| 대상 | 방식 |
|---|---|
| 일반 적 3종 | 피격 시에만 머리 위에 작은 바 표시, 1초 뒤 서서히 사라짐 |
| 준보스·보스 | 상단 중앙에 **고정** 표시 (사라지지 않음) |

보스전은 20초 이상 지속되므로 체력 바가 사라지면 진행도를 알 수 없어 불필요한 스트레스가 생긴다. A/B 양쪽에 동일하게 작동하므로 통제 변수로 안전하다.

**미니맵 없음.** 카메라는 방 하나가 화면에 통째로 들어오는 **고정 방식** (플레이어 추적 금지 — 시야 차이가 변수가 됨).

### ESC 메뉴

```
계속하기
맵 보기        ← 별도 화면. 열 때마다 로그에 카운트
조작법        ← WASD 이동 / 마우스 클릭 공격
그만하기      ← "정말 그만할래?" 확인 후 종료
```

메뉴가 열려 있는 동안 `Time.timeScale = 0`. **반드시.** 안 그러면 메뉴 켜둔 시간이 클리어 타임에 들어간다.

### 맵 — 표시 규칙이 엄격하다

**게임 화면에 미니맵을 절대 띄우지 말 것.** 맵은 ESC 메뉴 안의 별도 화면에서만 볼 수 있다.

| 상태 | 표시 |
|---|---|
| 클리어한 방 | 채워진 사각형 + 방 번호 |
| 현재 위치 | 강조 색 + 표식 |
| 클리어한 방과 문으로 연결된 **미탐색** 방 | 점선 윤곽 + `?` (번호도 숨김) |
| 그 너머 | **아예 그리지 않음** |

- 시작방(0번)은 적이 없으므로 처음부터 클리어 상태로 취급
- **일방통행 여부를 표시하지 말 것.** 방향까지 보이면 되돌림 복도가 함정으로 기능하지 못한다
- 문은 방향 없는 선으로만 표시

**설계 의도**: 완화 장치를 완전히 제거하는 대신, **접근 비용이 있는 형태**로만 제공한다. 플레이 중에는 여전히 아무 정보가 없고, 정말 막혔을 때만 메뉴를 열어 확인하게 된다. 그 **이용 빈도 자체가 지표**가 된다.

**로그**: 맵 화면을 열 때마다 `GameLogger.Instance.RecordMapView()` 호출. ESC 메뉴를 여는 것과는 별개로, 맵 항목을 눌렀을 때만 센다.

이 지표가 중요한 이유 — "길을 잃었다"를 설문 응답이 아니라 **행동으로 측정**할 수 있다. A와 B의 맵 확인 횟수가 갈리면 그 자체가 가설 지지 근거이며, 설문의 길잃음 점수와 교차 검증도 가능하다.

### 사망 화면

```
        죽었습니다

     [ 다시 시도 ]
     [ 포기하기 ]
```

- 다시 시도 → 현재 방 입구에서 **HP 60** 으로 부활, **방의 적 전원 리셋**
- 포기하기 → **확인 없이** 즉시 기록 후 종료 (ESC 메뉴와 다르게 처리. 이미 실패한 상태라 붙잡으면 데이터가 왜곡됨)
- 누적 사망 10회 초과 시 화면에 한 줄 추가: `계속 어려우면 포기하셔도 됩니다`

---

## 플레이어

| 항목 | 값 |
|---|---|
| 이동 속도 | 5 유닛/초 |
| HP | 100 |
| 피격 데미지 | 10 (탱커 20, 준보스 15, 보스 20) |
| 무적 시간 | 0.5초 |
| 공격 | 마우스 **꾹 누르기 자동 연사** (클릭 연타 요구 금지) |
| 연사 간격 | 0.5초 → 무기 획득 후 **0.3초** |
| 투사체 속도 | 15 유닛/초 (이동 속도의 3배) |
| 투사체 사거리 | 화면 절반쯤에서 소멸 |
| 투사체 충돌 | 벽에 닿으면 소멸, 적 1체 명중 시 소멸 (관통 없음) |

**넣지 말 것**: 탄약 제한, 에임 어시스트, 대시·구르기. 모두 변수를 추가하거나 조작 학습 부담을 늘린다.

---

## 적

총 30 = 근접 18 · 원거리 9 · 탱커 2 · 준보스 1 (최종 보스 별도). A/B 동일.

### 일반 적 3종

| | 근접 | 원거리 | 탱커 |
|---|---|---|---|
| HP (플레이어 피격 수) | 3발 | 3발 | 9발 |
| 이동 속도 | 3.5 | 2.5 | 2 |
| 데미지 | 10 | 10 | 20 |
| 행동 | 직선 추적 | 6유닛 거리 유지 | 직선 추적 |
| 발사 간격 | — | 1.5초 | — |

셋 다 플레이어(5)보다 느리다. **도망칠 수 있어야 한다** — 갇혀서 확정 사망하면 그 이탈이 배치 때문인지 전투 때문인지 구분 불가.

### 준보스 (방 7) / 보스 (방 8)

같은 프리팹, 변수만 다르게. 새로 만들지 말 것.

| 항목 | 준보스 | 보스 |
|---|---|---|
| HP | 18발 | 36발 |
| 전체 쿨타임 | 1.5초 | 1.5초 |
| 돌진 | 항상 | 8유닛 **초과** 시 |
| 샷건 | 없음 | 8유닛 **이하** 시 |
| 접촉 판정 | **상시** | **상시** |
| 접촉 데미지 | 15 | 20 |

**행동 루프**

```
거리 측정
  ├─ 8유닛 초과 → 돌진 (1초 조준 → 직선 돌진 → 2초 경직)
  └─ 8유닛 이하 → 샷건 (전면 부채꼴 60도, 5발, 사거리 8)
        ↓
    전체 쿨타임 1.5초
        ↓
     거리 재측정 (반복)
```

- 샷건 사거리 8 = 패턴 전환 거리 8. **일부러 일치시킨 것** — "8유닛 밖이 안전지대"라는 규칙이 하나로 통일됨
- 돌진은 자체 경직 2초가 있어 실질 대기 3.5초, 샷건은 1.5초 → 가까이 붙으면 공격이 더 자주 온다 (거리 유지에 보상)

---

## 방 구성

### 문 잠금

**방 입장 시 문이 잠기고, 적 전멸 시 개방.** 시작방(0번)만 예외로 항상 열림.

- 잠긴 문 = 빨간색, 열린 문 = 초록색 (**필수**. 안 보이면 버그로 오해해 포기함)
- 문 통과는 **순간이동 방식**: 열린 문에 닿으면 옆 방의 그 문 앞으로 바로 이동 (아이작 방식). 통로에 머무는 순간이 없어 입장 즉시 잠가도 끼지 않는다
- 문 개방은 색 변화로만 알림. 텍스트 알림은 아이템 전용 (겹치면 둘 다 못 읽음)

### 보상

**적 전멸 순간 자동 획득.** 바닥에 놓고 밟게 하지 말 것 — 못 보고 지나치면 화력·체력 차이가 배치 효과에 섞인다.

| 종류 | 효과 | 알림 문구 |
|---|---|---|
| 체력회복 | HP +60 (최대 100) | `체력 회복  +60` |
| 무기 | 연사 0.25 → 0.15초 | `무기 강화!  공격 속도 상승` |

### 시작방 (0번)

- 적 0마리, 보상 없음, **문 항상 열림**
- 바닥에 텍스트 (아이작의 번제 방식): `WASD 이동` / `마우스 클릭 공격` / `적을 모두 처치하면 문이 열립니다` / `ESC 메뉴`
- 강제로 붙잡지 않는다 — 머무는 시간이 사람마다 달라 총 플레이시간이 오염됨

### B 전용 — 되돌림 복도 (101 · 102 · 103)

B 버전에만 존재하는, 본 경로 위쪽에 가로로 이어진 방 3칸.

```
        102 ── 101 ── 103        ← 되돌림 복도
         │      │      │
         ↓      │      │
   1 ── 2 ── 3 ── 4 ── 5 ...     ← 본 경로
```

- **입구**: 방 3에서 위로, 또는 방 4에서 위로 (둘 다 양방향 — 되돌아 나올 수 있음)
- **출구**: 102에서 아래로 떨어지면 **방 2** (일방통행). 두 칸 뒤로 밀려난다
- 각 칸에 근접 적 3마리, 보상 없음, 문 잠김 적용

**설계 의도**: 막다른 길이 아니라 "가긴 가는데 뒤로 간다". 플레이어는 진행 경로인지 아닌지 모르는 상태로 선택하며, 잘못 고르면 전투 + 후퇴라는 이중 비용을 치른다.

**최단 경로는 여전히 8방이다.** 어떤 경로로도 지름길이 생기지 않는다:
- 방3 → 101 → 103 → 방4 = 3칸 (직행 1칸보다 길다)
- 방4 → 103 → 101 → 102 → 방2 = 뒤로 이동

### 클리어한 방의 재진입

**한 번 클리어한 방은 적이 다시 나오지 않고 문도 열린 채 유지된다.** (사망 시 부활은 예외 — 그 방만 리셋)

이게 없으면 되돌아갈 때마다 재전투가 발생해, 측정 대상인 "이동 비용"이 아니라 "전투량 차이"가 결과에 섞인다.

---

## 타이머 — 중요

**총 플레이시간은 방 1에 처음 진입한 순간부터 측정한다.** 시작 버튼이 아니다.

시작방에서 조작법을 읽는 시간이 사람마다 다르므로, 그 시간이 클리어 타임에 섞이면 안 된다.

```csharp
public void StartTimerAtRoom1()
{
    if (timerStarted) return;   // B에서 되돌아와도 재시작 금지
    startTime = Time.time;
    timerStarted = true;
}
```

- `BeginSession()`(참가자 번호 입력)과 **타이머 시작을 분리**할 것
- 방 1의 `RoomTrigger`가 처음 발동할 때 호출
- 시작방 체류 시간은 **별도로 기록** (CSV `시작방체류시간` 열)

---

## 아키텍처 — 데이터 주도 방식

방을 씬에 손으로 배치하지 말고 **JSON을 읽어 런타임 생성**한다. A/B 전환이 파일 교체로 끝나야 한다.

```
Assets/
├── Scripts/
│   ├── GameLogger.cs          로그 수집 (제공됨)
│   ├── RoomTrigger.cs         방 진입 감지 (제공됨)
│   ├── TitleScreen.cs         타이틀 화면 (코드 생성, TMP 불필요 — 원본 SessionStartUI 는 docs/미사용_TMP버전/)
│   ├── MapData.cs             JSON 역직렬화
│   ├── MapBuilder.cs          핵심: 방/문/적/보상 생성
│   ├── PlayerController.cs
│   ├── Projectile.cs
│   ├── EnemyBase.cs / MeleeEnemy.cs / RangedEnemy.cs / TankEnemy.cs
│   ├── BossController.cs      준보스·보스 공용
│   ├── DoorController.cs      잠금·일방통행 처리
│   ├── PauseMenu.cs           ESC 메뉴
│   ├── DeathScreen.cs
│   └── ItemToast.cs           획득 알림
├── Resources/
│   ├── map_A.json
│   └── map_B.json
└── Prefabs/
    ├── Room, Wall, Door
    ├── Player, Projectile
    ├── Enemy_Melee, Enemy_Ranged, Enemy_Tank, Boss
    └── (보상은 프리팹 불필요 — 자동 획득)
```

### JSON 스키마

```json
{
  "version": "A",
  "roomSize":  { "w": 16, "h": 10 },
  "spacing":   { "x": 18, "y": 12 },
  "rooms": [
    {
      "id": 0,
      "grid": [-1, 0],
      "type": "tutorial",
      "isDeadEnd": false,
      "lockDoors": false,
      "enemies": { "melee": 0, "ranged": 0, "tank": 0 },
      "boss": null,
      "reward": null
    }
  ],
  "doors": [
    { "from": 0, "to": 1, "oneWay": false }
  ]
}
```

- `grid` × `spacing` = 월드 위치
- `type`: `tutorial` / `start` / `normal` / `boss`
- `boss`: `null` / `"mini"`(준보스) / `"final"`(보스)
- `reward`: `null` / `"heal"` / `"weapon"`
- `oneWay: true` → `from`에서 `to` 방향으로만 통과

### MapBuilder 가 할 일

1. `Resources/map_{version}.json` 로드 (version은 `GameLogger.mapVersion`)
2. 각 방을 격자 좌표 × spacing 위치에 생성
3. `RoomTrigger` 부착 — `roomNumber`, `isDeadEnd` 를 JSON에서 채움
4. 인접 방 사이에 문 생성. **`doors` 배열에 없는 인접 쌍은 벽으로 막을 것**
5. 적을 방 안에 겹치지 않게 배치
6. 플레이어를 `tutorial` 방에 스폰

### 적 배치 랜덤성

좌표는 랜덤이어도 되지만 **A와 B에서 같은 방은 같은 배치가 나와야 한다.** 방 번호를 시드로:

```csharp
var rng = new System.Random(roomId * 7919);
```

---

## 기술 설정

```csharp
Screen.SetResolution(1280, 720, FullScreenMode.Windowed);
Application.targetFrameRate = 60;
QualitySettings.vSyncCount = 0;
```

- **모든 이동은 `Time.deltaTime` 기반** (프레임 차이가 속도에 영향을 주면 안 됨)
- 카메라 `orthographicSize`도 코드로 고정

---

## 로그 연동

| 호출 | 위치 |
|---|---|
| `EnterRoom(id, isDeadEnd)` | RoomTrigger가 자동 처리 |
| `StartTimerAtRoom1()` | 방 1 최초 진입 시 |
| `RecordDeath(transform.position)` | 플레이어 사망 시 |
| `RecordMapView()` | ESC 메뉴에서 맵 화면을 열 때 |
| `FinishCleared()` | 보스 처치 시 |
|  `FinishGaveUp()` | 사망 화면·ESC 메뉴의 포기 |

### CSV 열

**summary.csv** (참가자 1명 = 1행)

```
기기번호, 번호, 버전, 총플레이시간, 클리어시간, 시작방체류시간,
사망횟수, 되돌아간횟수, 맵확인횟수, 포기여부, 포기시점, 되돌림복도진입, 평균FPS, 최저FPS
```

- 포기자는 클리어시간을 **빈칸**으로 (0으로 두면 평균이 망가짐)
- 평균FPS·최저FPS는 성능 차 사후 검증용. 1초 단위로 샘플링

**deaths.csv** (사망 1회 = 1행)

```
기기번호, 번호, 버전, 몇번째죽음, x, y, 방번호
```

적이 리셋되므로 같은 좌표가 반복해서 찍힌다. **중복 제거하지 말 것** — 밀도가 병목 지점을 드러낸다.

---

## 작업 순서

각 단계마다 **플레이 가능한 상태를 유지**할 것.

1. MapData + MapBuilder + JSON 로딩 — 방과 문만. 적·보상 없이 먼저 확인
2. PlayerController — 이동, 공격, HP, 사망
3. DoorController — 잠금, 개방, 색 변화, 일방통행
4. 적 3종 (EnemyBase 상속)
5. BossController — 준보스·보스 공용
6. 보상 자동 획득 + ItemToast
7. UI — 방 번호, HP 바, 적 체력 바, ESC 메뉴, 맵 화면, 사망 화면
8. 로그 연동 + FPS 측정

---

## 완료 판정 체크리스트

- [ ] map_A.json 으로 시작방 + 방 8개가 일자로 생성된다
- [ ] map_B.json 으로 시작방 + 방 10개, 갈림길 3개가 생성된다
- [ ] B의 방 6 → 방 3 통로가 **한 방향으로만** 통과된다
- [ ] 시작방 문은 잠기지 않고, 나머지 방은 적 전멸 시 열린다
- [ ] 잠긴 문과 열린 문의 색이 다르다
- [ ] 타이머가 **방 1 진입 시점**부터 돈다 (시작 버튼 아님)
- [ ] 방 3·5·7 전멸 시 보상이 자동 적용되고 알림이 뜬다
- [ ] 죽으면 방 입구에서 부활하고 그 방의 적이 전부 리셋된다
- [ ] 사망 화면의 포기하기가 확인 없이 즉시 종료된다
- [ ] ESC 메뉴에서 시간이 멈춘다
- [ ] 보스가 8유닛을 기준으로 돌진/샷건을 전환한다
- [ ] 창이 1280×720 으로 뜨고 전체화면이 안 된다
- [ ] **A와 B의 총 적 수가 각각 30이다** (콘솔에 출력해 확인)
- [ ] **게임 화면에 미니맵이 없다** (맵은 ESC 메뉴 안에서만)
- [ ] 맵에 클리어한 방과 인접 미탐색 방만 보이고, 그 너머는 안 보인다
- [ ] 맵에 일방통행 방향이 표시되지 않는다
- [ ] 맵을 열면 맵확인횟수가 오른다
- [ ] **되돌림 복도에 보상이 없다**
- [ ] 화면에 그만하기 버튼이 없고, ESC 메뉴로 종료할 수 있다
- [ ] 일반 적은 피격 시 체력 바가 떴다가 1초 뒤 사라지고, 보스는 상단에 고정된다
- [ ] 클리어한 방에 다시 들어가도 적이 리스폰되지 않는다
- [ ] B에서 102 에서 떨어지면 방 2 로 나온다

마지막 세 개는 실험 타당성에 직결된다. 반드시 눈으로 확인할 것.

---

## 시간이 부족하면 버려도 되는 것

버려도 되는 순서:

1. 보스 샷건 패턴 → 돌진만으로도 성립
2. 맵의 점선 미탐색 표시 → 클리어한 방만 그려도 됨
2. 아이템 알림·적 체력 바의 페이드 애니메이션 → 즉시 표시/제거로
3. 적 사망 이펙트, 사운드
4. 타이틀 화면 꾸미기

**절대 버리면 안 되는 것**: 방 번호 표시, ESC 메뉴, 문 잠금 색 구분, 로그 저장, 방 1 타이머 시작. 이 중 하나라도 없으면 실험이 성립하지 않는다.
